# Build-only values. This file contains no production secret and is never used at runtime.
NODE_ENV=production
APP_ENV=production
APP_BASE_URL=https://gestion.crisoft.es
DATABASE_URL=postgresql://build_only:build_only@127.0.0.1:5432/crigestion_build?schema=public
APP_SESSION_SECRET=build-only-placeholder-at-least-32-characters
VERIFACTU_CREDENTIAL_IDEMPOTENCY_SECRET=build-only-distinct-placeholder-32-characters
AUTH_COOKIE_SECURE=true
TRUST_PROXY_HEADERS=true
VERIFACTU_ENABLED=false
VERIFACTU_ALLOW_PRODUCTION=false
VERIFACTU_WORKER_ALLOW_PRODUCTION=false
