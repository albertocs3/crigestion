# Install as /etc/crigestion-staging/recovery-bundle.env (root:root, mode 0600).
# This identifier is public metadata. The master key is a separate encrypted
# systemd credential and must never be written in this file.
RECOVERY_BUNDLE_KEY_ID=CHANGE_ME_RECOVERY_KEY_ID
