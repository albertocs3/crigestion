import { jsonResponse, validationError } from "@/modules/platform/application/http"; import { registerSupplierPayment, registerSupplierPaymentSchema } from "@/modules/purchases/application/purchases"; import { authorizePurchaseMutation } from "../../purchases/_http";
export const runtime = "nodejs"; export async function POST(request: Request) { const auth = await authorizePurchaseMutation(request, "Treasury.ManageSupplierPayments"); if (!auth.ok) return auth.response; const parsed = registerSupplierPaymentSchema.safeParse(auth.body); if (!parsed.success) return jsonResponse(request, validationError(parsed.error.flatten()), { status: 422 }); const result = await registerSupplierPayment(parsed.data, auth.user, { correlationId: auth.correlationId, idempotencyKey: auth.idempotencyKey, requestHash: auth.requestHash, scope: "supplier-payment" }); return jsonResponse(request, result.ok ? result.value : result.error, { status: result.status }); }
