import { z } from "zod"; import { jsonResponse, validationError } from "@/modules/platform/application/http"; import { getPurchase, updatePurchase, updatePurchaseSchema } from "@/modules/purchases/application/purchases"; import { authorizePurchaseMutation, authorizePurchaseRead } from "../_http";
const paramsSchema = z.object({ purchaseId: z.string().uuid() }); export const dynamic = "force-dynamic"; export const runtime = "nodejs";
export async function GET(request: Request, context: { params: Promise<{ purchaseId: string }> }) { const params = paramsSchema.safeParse(await context.params); if (!params.success) return jsonResponse(request, validationError(params.error.flatten()), { status: 422 }); const auth = await authorizePurchaseRead(request); if (!auth.ok) return auth.response; const result = await getPurchase(params.data.purchaseId, auth.user); return jsonResponse(request, result.ok ? result.value : result.error, { status: result.status }); }
export async function PATCH(request: Request, context: { params: Promise<{ purchaseId: string }> }) { const params = paramsSchema.safeParse(await context.params); if (!params.success) return jsonResponse(request, validationError(params.error.flatten()), { status: 422 }); const auth = await authorizePurchaseMutation(request, "Purchases.ManageDrafts"); if (!auth.ok) return auth.response; const parsed = updatePurchaseSchema.safeParse(auth.body); if (!parsed.success) return jsonResponse(request, validationError(parsed.error.flatten()), { status: 422 }); const result = await updatePurchase(params.data.purchaseId, parsed.data, auth.user, { correlationId: auth.correlationId, idempotencyKey: auth.idempotencyKey, requestHash: auth.requestHash, scope: `update:${params.data.purchaseId}` }); return jsonResponse(request, result.ok ? result.value : result.error, { status: result.status }); }
